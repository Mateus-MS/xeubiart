name: Publish Xeubiart Packages

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      version:
        description: 'Version tag to publish (e.g., v1.0.0)'
        required: true
        default: 'v1.0.0'

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.CR_PAT }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Extract Version Tag
        id: vars
        run: |
          VERSION=${GITHUB_REF#refs/tags/v}
          echo "VERSION=$VERSION" >> $GITHUB_OUTPUT

      - name: Build and Push Templ Service
        uses: docker/build-push-action@v6
        with:
          context: frontend/templ
          file: frontend/templ/Dockerfile
          push: true
          tags: |
            ghcr.io/mateus-ms/xeubiart-templ:${{ steps.vars.outputs.VERSION }}
            ghcr.io/mateus-ms/xeubiart-templ:latest

      - name: Build and Push Caddy Service
        uses: docker/build-push-action@v6
        with:
          context: .
          file: infra/caddy/Dockerfile
          push: true
          tags: |
            ghcr.io/mateus-ms/xeubiart-caddy:${{ steps.vars.outputs.VERSION }}
            ghcr.io/mateus-ms/xeubiart-caddy:latest